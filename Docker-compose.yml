services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./upload:/app/upload
    env_file:
      - .env
    extra_hosts:
      - "localhost:host-gateway"
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

