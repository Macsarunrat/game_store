import { StaticUrlPipe } from './static-url-pipe';

describe('StaticUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
