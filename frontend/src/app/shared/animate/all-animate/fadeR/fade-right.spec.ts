import { FadeRight } from './fade-right';

describe('FadeRight', () => {
  it('should create an instance', () => {
    const directive = new FadeRight();
    expect(directive).toBeTruthy();
  });
});
